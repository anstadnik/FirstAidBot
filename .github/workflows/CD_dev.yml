name: CI/CD dev

on:
  push:
    branches:
      - "dev"

jobs:
  build_and_deploy:
    uses: ./.github/workflows/CI_CD.yml
    with:
      release: false
      deploy: true
      environment: test
    secrets: inherit
